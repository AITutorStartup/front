# Используем прокси Vite для обхода CORS при разработке
# Запросы к /api/* будут проксироваться на http://194.67.99.120:8000/*
VITE_API_URL=/api

# DEV: показывать код подтверждения из ответа бэкенда (пока почта не работает)
# Убрать или установить в false когда почта заработает
VITE_SHOW_DEV_CODE=true
